export interface StudentModel {
    id: 3,
    surname: string | null,
    name: string | null,
    patronymic: string | null,
    email: string | null,
    phone: string | null,
    group: string | null,
    course: 4,
}
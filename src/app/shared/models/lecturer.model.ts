export interface LecturerModel {
    id: number,
    surname: string | null,
    name: string | null,
    patronymic: string | null,
    email: string | null,
    phone: string | null,
    login: string | null,
    password: string | null,
}